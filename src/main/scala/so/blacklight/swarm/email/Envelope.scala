package so.blacklight.swarm.email

/**
	* An Envelope instance contains transmission and delivery information about a specific email,
	* such as the sender and the recipient(s) of the email.
	*
	*/
class Envelope(sender: EmailAddress, recipient: Seq[EmailAddress], message: Message) {

}
