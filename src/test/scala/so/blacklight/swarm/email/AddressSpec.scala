package so.blacklight.swarm.email

import org.scalatest.FunSpec
import so.blacklight.swarm.email.meh.{Address, Domain}

/**
	*
	*/
class AddressSpec extends FunSpec {

	/*
	describe("apply") {
		it ("should create new EmailAddresses") {
			val maybeUser = User("user")
			val maybeDomain = Domain("domain")

			val maybeAddress = Address(maybeUser, maybeDomain)

			assert(maybeAddress.isRight)
		}

		it ("should not accept empty strings as addresses") {
			val maybeAddress = Address("")

			assert(maybeAddress.isLeft)
		}
	}
	*/

}
